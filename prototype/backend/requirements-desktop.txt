# Desktop window (pywebview). Backend runtime deps (flask, numpy, scipy, …)
# are assumed already installed in the eventcamera-blender env.
#
#   pip install -r requirements-desktop.txt
#
# Linux also needs the system WebKitGTK packages, e.g. on Ubuntu:
#   sudo apt install python3-gi gir1.2-webkit2-4.1 libgirepository1.0-dev
#   pip install "pywebview[gtk]"
# Windows uses the WebView2 runtime (preinstalled on Win11 / recent Win10).
# macOS uses the built-in WKWebView (no extra install).

pywebview>=5.0
